---
description: 
globs: 
alwaysApply: true
---
# GIT COMMIT MESSAGE 规范

当项目是基于 Monorepos 管理时，在生成 git commit message 时，请遵循以下规范。

## 首先是 git-flow 模式的 commit-message 具体规则如下

<type>(<scope>): <description>

### commit-message 中使用的 type 参考

-   feat：新功能的实现（feature）。
-   fix：修复 bug。
-   docs：文档修改（如 README 或 API 文档）。
-   style：代码格式修改（不影响代码逻辑），例如空格、缩进、分号等。
-   refactor：代码重构，不改变功能，改善代码结构。
-   perf：性能优化。
-   test：添加或修改测试代码。
-   chore：其他杂项任务，如构建过程、工具配置等。

### commit-message 中使用的 scope 要求

在 Git Flow 的提交规范下，scope 主要用于标识提交影响的模块或子项目。在 Monorepo 中，scope 可以是以下几种表示方式

1. 模块或子项目名称

每个子项目或模块在 Monorepo 中都应该有一个明确的名称，scope 用来指代它。 scope 就是该模块所在子项目名称。

示例：
假设你的 Monorepo 中有多个模块，比如 frontend、backend 和 shared，则提交时的 scope 可以这样写：

feat(frontend): add new user profile page
fix(backend): resolve database connection issue
chore(shared): update utility functions

-   frontend 表示前端模块。
-   backend 表示后端模块。
-   shared 表示共享的库或功能模块。

2. 使用路径表示 Scope

如果 Monorepo 中的模块存放在不同的子目录下，可以通过子目录路径来表示模块。例如，frontend/src/ 可能对应前端模块，backend/db/ 可能对应数据库模块。

示例：
feat(frontend/src/components): add new navigation bar
fix(backend/db/migrations): fix migration issue

-   frontend/src/components：表示前端模块中的某个组件目录。
-   backend/db/migrations：表示后端模块中的数据库迁移部分。

3. 跨模块的更新

如果你的提交同时影响多个模块，`scope` 可以包含多个模块名，或者使用描述性词语来标识它们之间的关系。

示例：
feat(frontend, backend): implement user authentication flow
chore(frontend, shared): update design system components

-   frontend, backend：表示提交同时影响前端和后端模块。
-   frontend, shared：表示提交涉及前端和共享组件。

4. 版本号作为 Scope（用于版本管理）

在某些情况下，特别是在共享库或者内部包管理的 Monorepo 中，`scope` 也可以是特定的包名称或版本号，表示对某个依赖的变更。

chore(shared-utils): bump version to 2.0.0

-   shared-utils：表示在共享工具库中的版本更新。

5. 子模块或功能模块

如果你使用的是像 **Lerna**、**Yarn Workspaces** 这样的工具来管理多个包，也可以用具体的包名作为 `scope`。

示例：
feat(core-ui): add dark mode support
fix(api-client): resolve token expiration bug

-   core-ui：表示 UI 核心组件库。
-   api-client：表示 API 客户端模块。

## commit-message 中使用的 description

description 需要使用中文。要求如下：

1. 简短：标题应该简洁明了，通常不超过 50 个字符。
2. 使用祈使句：标题应该使用祈使句（例如，“添加”、“修复”、“更新”等），表示对项目的直接行动。
3. 避免使用过去时：如 "fixed"、"added" 等。标题应表示一个动作，而非已完成的动作。
4. 避免过于模糊：不要使用过于模糊的描述，比如 "fix bugs" 或 "change things"。

## 其次如果有必要（功能改动较大）可以添加正文详细描述这次提交的背景、动机和实现方式，必要时可以包含解决的问题、原因等。通常每行不超过 72 个字符。

正文要求如下

1. 解释“为什么”和“如何”：正文应该解释这次提交为什么需要进行（背景信息）以及如何实现（技术细节）。这有助于团队成员理解提交的动机和解决方案。
2. 分段描述：如果正文较长，建议使用空行将不同的部分分开（如问题描述、解决方案、影响等）。
3. 强调核心变更：详细说明代码中较为复杂、需要注意的地方，或者实现的方案和设计思想。

示例：
fix(button): adjust size and padding for mobile devices

The button's size and padding were off on mobile devices, causing
usability issues. Increased the button's padding and adjusted its
font size to make it more touch-friendly on smaller screens.

## 综合示例

假设在一个 Monorepo 项目中，包含了 `frontend`、`backend`、`shared` 和 `core-ui` 四个模块，你的 Git Commit 可能会有如下的表示：

#### 1. **增加新功能**

```plaintext
feat(frontend/auth): add user login page
feat(backend/api): implement user registration endpoint
```

#### 2. **修复 bug**

```plaintext
fix(backend/database): fix query optimization issue
fix(shared-utils): resolve date formatting bug
```

#### 3. **文档更新**

```plaintext
docs(frontend): update README with new setup instructions
docs(shared-utils): add usage examples for helper functions
```

#### 4. **重构代码**

```plaintext
refactor(backend): refactor authentication service
```

#### 5. **性能优化**

```plaintext
perf(frontend): optimize image loading performance
```

### 总结

在 **Monorepo** + **Git Flow** 工作流中，**`scope`** 的作用是标识提交影响的具体模块或子项目。通过使用清晰的模块名称或路径来标识 `scope`，你能够：

-   **明确每个提交的范围**，特别是在多个模块同时进行开发时。
-   **减少冲突**，帮助开发者理解提交的上下文。
-   **提高可维护性**，使得代码库中的变更更加可追溯和管理。

### 关键点：

-   **scope** 应该反映变更的模块或子项目。
-   使用清晰的模块名或目录路径来表示 scope。
-   对于跨多个模块的提交，可以使用多个模块名称来组合表示。
