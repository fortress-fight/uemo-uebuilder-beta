import { install as LabelInstall } from "~/packages/label-directive";
import { install as DraggerInstall } from "~/packages/dragger-directive";

export const directive = { label: LabelInstall, dragger: DraggerInstall };
