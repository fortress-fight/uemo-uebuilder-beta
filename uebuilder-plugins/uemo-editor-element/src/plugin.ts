import { install as ToastInstall } from "~/packages/toast-plugin";
import { install as FileUploadInstall } from "~/packages/file-upload-plugin";

export const plugins = { toast: ToastInstall, fileUpload: FileUploadInstall };
