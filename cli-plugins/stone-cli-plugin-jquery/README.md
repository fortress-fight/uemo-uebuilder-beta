# `stone-cli-plugin-jquery`

> 添加 jquery 组件

## 功能

`stone-cli-plugin-jquery` 提供了下列功能

1.  添加 jquery 组件
2.  暴露全局变量 `$`

## 使用方式

在 `package.json` 中添加

```json
{
    "devDependencies": {
        "@stone/vue-cli-plugin-jquery": ...,
    }
}
```

执行

```shell
yarn
```
